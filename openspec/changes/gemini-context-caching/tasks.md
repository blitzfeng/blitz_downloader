## 1. Prompt 顺序调整

- [ ] 1.1 在 `GeminiProvider.buildTagSuggestionPrompt` 里把 `desc`(视频文案)那一段从"指令说明之后、标签词表之前"挪到"标签词表 JSON 输出之后、作者历史先验之前"。人工核对改动后拼出的完整 prompt 字符串,确认信息内容与改动前完全一致(逐段比对文本,只是顺序变了,没有任何一段被删除或改写)。
- [ ] 1.2 跑 `./gradlew testDebugUnitTest` 里覆盖 `llm` 包的既有测试(如有)与 `./gradlew assembleDebug`,确认编译通过、不引入回归。

## 2. 真机验证隐式缓存是否生效

- [ ] 2.1 真机安装改动后的 debug 包,清空 `adb logcat` 缓冲区,短时间内(几分钟内)连续对 3~5 条不同视频点击「AI 建议」。
- [ ] 2.2 `adb logcat` 按 TAG `GeminiProvider` 过滤,核对每次响应体日志里 `usageMetadata` 段落:记录 `cachedContentTokenCount` 字段是否出现、出现时的数值,以及 `promptTokenCount` 相比未启用隐式缓存前是否有下降趋势(第一次请求预期不会命中,后续请求若命中应能看到该字段出现且非零)。
- [ ] 2.3 把验证结果(命中/未命中,及可能的原因猜测,如"3.8 暂不支持隐式缓存"或"点击间隔超出服务端窗口")记录下来,作为是否需要发起「显式缓存」后续变更的依据——不在本次变更里做进一步动作,只产出这份验证结论。

## 3. 文档更新

- [ ] 3.1 更新 `CLAUDE.md`「AI 建议标签」小节里"Context Caching(用户提出、暂未实现)"这段:改为描述本次已实现的隐式缓存启用条件(prompt 顺序调整),并注明显式缓存的最小 token 门槛(4096 官方文档 vs 32768 未经证实的说法)尚未定论、留给后续独立变更真机验证后再定,避免以后又有人凭这两个数字里的任意一个直接动手写死代码。
