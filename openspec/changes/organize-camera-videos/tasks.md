## 1. 筛选规则与目录访问

- [x] 1.1 实现视频及图片类型判断、各自前缀规则和冲突序号命名；通过 JVM 测试覆盖 IMG、img、Vid、video、普通视频、图片、未知类型及多次同名。
- [x] 1.2 实现主存储固定目录扫描和候选快照，禁止递归及越界链接；使用临时目录测试验证子目录排除、空目录、源目录不存在和不可读错误的区分。
- [ ] 1.3 接入 Android 11+ 所有文件访问权限及 Android 6–9 读写权限分支；设备验证拒绝、撤回和授权返回后仅扫描预览、不直接移动。
- [ ] 1.4 实现 Android 10 SAF 双目录授权及存储适配；在 Android 10 验证正确目录访问、错误目录拒绝、取消授权及持久授权失效处理。

## 2. 文件移动与批次执行

- [x] 2.1 实现目录创建、不覆盖提交、同名序号和复制校验后删除源文件；使用临时文件及失败注入验证目标内容完整、已有目标不变、写入或校验失败保留源、删除失败报告副本位置。
- [x] 2.2 实现候选重检、单任务互斥、结构化进度和逐项结果；测试预览后文件变更、重复确认、部分失败、权限撤回及成功/跳过/失败/未处理计数守恒。
- [ ] 2.3 实现临时文件身份记录及受限清理，禁止自动恢复移动或源删除；模拟复制中断和进程重启，验证源保留、残留可定位且不删除非本功能文件。
- [ ] 2.4 实现源目标媒体索引同步并将刷新失败与文件移动结果分开；设备验证相册旧路径更新、目标可播放、现有隐藏规则及应用下载记录不变。

## 3. 设置页交互

- [ ] 3.1 新增 Compose 设置入口与预览确认界面，展示规则、目标、总数、总大小和完整文件名列表；手动验证打开设置不操作文件、无候选提示、取消后不创建目标目录。
- [ ] 3.2 接入进度、结果及逐项失败原因，使用现有主题、弹窗基类和结果契约；验证屏幕旋转、tab 切换、重复点击不会启动新批次，并能继续查看已有进度和结果。

## 4. 集成验证与说明

- [ ] 4.0 验证图片扩展：图片排除 img、mvimg、pano、retouch 前缀并移入 history_img，视频仍移入 history；用混合文件批次测试 VID 图片、大小写保留、各目录同名不覆盖和逐项目标预览。

- [x] 4.1 运行相关 JVM 测试和 `./gradlew.bat :app:assembleDebug`，记录命令与结果，确认编译通过且文件保护用例无失败。
- [ ] 4.2 按规范场景完成 Android 6–9、Android 10、Android 11+ 设备或模拟器验收，重点覆盖真实公共目录、同名、空间不足、外部变更及中断；交付验收记录，未验证项明确标注。
- [x] 4.3 更新项目功能说明与适用权限说明，记录文件名识别局限、失败副本处理及手工移回方式；核对文档与最终界面、实现一致，并运行 `openspec validate organize-camera-videos --strict`。

## 验证状态

实现与测试结果见 [verification.md](verification.md)。设备权限、SAF、媒体索引与界面验收尚未执行，对应任务保留未勾选；图片分目录核心已通过混合批次测试，预览布局仍待设备验收。
