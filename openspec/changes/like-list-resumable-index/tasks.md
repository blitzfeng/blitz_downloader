## 1. 持久化索引数据结构

- [ ] 1.1 定义来源身份、索引会话状态和索引条目的 Room 实体，并对来源与 aweme ID 建立唯一约束；验证结构包含可空的未来收藏夹标识，但不启用收藏夹索引。
- [ ] 1.2 新增 DAO：支持分页检查点事务、有序索引条目查询、会话查找和安全替换重新索引；验证 DAO 测试覆盖来源隔离和重叠 aweme ID。
- [ ] 1.3 提升 `AppDatabase` 版本并新增前向迁移，保留所有既有下载媒体与标签数据；验证迁移与数据库结构测试通过。

## 2. 点赞列表索引服务

- [ ] 2.1 实现点赞列表索引协调器：从游标零开始、使用 `count=50`，并在达到配置的唯一条目上限或远端列表结束时停止；验证其跟随响应游标而非推导页码。
- [ ] 2.2 每个成功分页都在同一事务中持久化唯一条目和下一游标；验证中断任务从保存游标恢复，并按 aweme ID 去除重叠分页。
- [ ] 2.3 检测认证失败、异常续拉数据和矛盾空页；将会话置为需要恢复状态且保留原有条目，并通过单元测试验证每条恢复路径。
- [ ] 2.4 在将持久化条目提交下载任务前，为选中条目重新解析媒体信息；验证过期的索引元数据不会被当作带签名媒体 URL 使用。

## 3. 列表下载集成

- [ ] 3.1 在点赞列表中增加最大索引条数选择，以及启动、恢复、显式重新索引会话的控制入口；验证 UI 从不把该值标记为 `max_cursor` 或页码。
- [ ] 3.2 展示索引进度、达到上限完成、远端完成和需要恢复等状态；验证进程重建后部分索引仍可浏览和选择。
- [ ] 3.3 通过分页数据库查询渲染已索引点赞条目，并提供来源顺序与作品创建时间升序；验证切换排序不需要在内存中重新构建远端列表。
- [ ] 3.4 保持作品、收藏、收藏夹和合集列表的既有行为不变；验证其现有列表加载测试和人工流程仍使用瞬态分页列表路径。

## 4. 验证与文档

- [ ] 4.1 添加纯 JVM 测试，覆盖索引上限、游标检查点/恢复、重复边界、恢复状态、来源隔离和排序；验证 `./gradlew.bat testDebugUnitTest` 通过。
- [ ] 4.2 使用跨多页的已登录点赞列表进行索引，中断后恢复，并覆盖恢复/重新索引路径；验证索引数量和选中的下载记录不含重复 aweme ID。
- [ ] 4.3 更新 `CLAUDE.md`，说明不透明游标、持久化索引和新鲜媒体解析约束；验证文档明确区分当前点赞支持和未来收藏夹扩展。
